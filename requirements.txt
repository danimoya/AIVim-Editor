# Core dependencies (aligned with pyproject.toml)
flask>=2.0.0,<4.0.0
openai>=1.0.0,<3.0.0
requests>=2.25.0,<3.0.0

# Optional AI providers
anthropic>=0.25.0,<1.0.0

# Optional local LLM support
llama-cpp-python>=0.2.0

# Configuration and utilities
python-dotenv>=1.0.0
configparser>=5.3.0
tqdm>=4.65.0

# Windows support
windows-curses>=2.3.0;platform_system=='Windows'

# Server deployment
gunicorn>=23.0.0

# Development and testing dependencies
# (For production, install with: pip install aivim[dev])
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-timeout>=2.1.0
coverage>=7.0.0